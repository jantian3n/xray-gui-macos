Place the macOS Xray binary here before packaging the Flutter app.

- macOS: assets/bin/macos/xray

You can install a local binary into this directory with:

  bash ./scripts/build_desktop_xray.sh /absolute/path/to/xray
